# x-parser 任务清单

语法分析器 - 从词法单元流构建 AST。

## 已完成 ✅

| 任务 | 状态 |
|------|------|
| 递归下降解析框架 | ✅ 已完成 |
| 表达式优先级爬升算法 | ✅ 已完成 |
| 完整 AST 结构定义 | ✅ 已完成 |
| 变量声明 `let x: T = expr` 解析 | ✅ 已完成 |
| 函数声明 `function name(params): T { ... }` 解析 | ✅ 已完成 |
| `async` 函数声明框架 | ✅ 已完成 |
| 控制流 `if`/`else` 解析 | ✅ 已完成 |
| `while` 循环解析 | ✅ 已完成 |
| `for` 循环解析框架 | ✅ 已完成 |
| `match` 模式匹配（语句级）解析 | ✅ 已完成 |
| `try`/`catch`/`finally` 解析 | ✅ 已完成 |
| `module`/`import`/`export` 声明解析 | ✅ 已完成 |
| 所有运算符正确优先级 | ✅ 已完成 |
| 类型注解解析 | ✅ 已完成 |
| 泛型参数 `<T, U>` 解析 | ✅ 已完成 |
| 复合字面量：数组 `[1, 2, 3]` | ✅ 已完成 |
| 复合字面量：字典 `{key: value}` | ✅ 已完成 |
| 或模式 `p1 | p2` 支持 | ✅ 已完成 |
| 模式守卫 `if guard` 支持 | ✅ 已完成 |

## 已完成 ✅

| 任务 | 状态 |
|------|------|
| 类声明解析 | ✅ 已完成 |
| 特质声明解析 | ✅ 已完成 |
| 效果系统语法解析 | ✅ 已完成 |
| 所有 SPEC 语法支持 | ✅ 已完成 |

## 待完成 ⬜

| 序号 | 任务 | 状态 | 优先级 | 描述 |
|------|------|------|--------|------|
| 1 | `defer` 语句解析 | ✅ 已完成 | 高 | SPEC 定义 defer 关键字 |
| 2 | `yield` 语句解析（生成器） | ✅ 已完成 | 高 | SPEC 定义 yield 关键字 |
| 3 | `loop` 关键字解析 | ✅ 已完成 | 中 | SPEC 定义 loop 无限循环 |
| 4 | 可选链 `?.` 语法解析 | ✅ 已完成 | 高 | SPEC 定义可选链成员访问 |
| 5 | 空合并 `??` 语法解析 | ✅ 已完成 | 高 | SPEC 定义空合并表达式 |
| 6 | `implement` Trait for Type 解析 | ✅ 已完成 | 高 | 解析 trait 实现声明 |
| 7 | 错误传播 `expr?` 语法解析 | ✅ 已完成 | 高 | SPEC 定义，用于 Result/Option 提前返回 |
| 8 | 通配符模式 `_` 解析 | ✅ 已完成 | 高 | match 模式匹配中的通配符 |
| 9 | 构造器模式（枚举变体解构）解析 | ✅ 已完成 | 高 | 支持 `Option.Some(x)` 模式 |
| 10 | 完整枚举声明解析 | ✅ 已完成 | 高 | 支持单元/元组/记录三种变体格式 |
| 11 | 完整记录声明解析 | ✅ 已完成 | 高 | 支持命名字段 `record Point { x: Int, y: Int }` |
| 12 | 完整类声明解析（构造函数/方法/继承/可见性） | ✅ 已完成 | 高 | 支持修饰符/构造函数/方法/字段/可见性完整实现 |
| 13 | 完整特质声明解析 | ✅ 已完成 | 高 | 支持泛型/继承/方法签名完整实现 |
| 14 | 效果声明 `effect` 解析 | ✅ 已完成 | 中 | 支持泛型/操作声明 `effect E { op: Input -> Output }` |
| 15 | `async`/`await` 完整表达式解析 | ✅ 已完成 | 中 | `async function` 已支持 + `await expr` 表达式解析完成 |
| 16 | 并发关键字 `concurrently`/`race`/`atomic`/`retry` | ✅ 已完成 | 中 | 支持 `wait together`/`wait race`/`wait atomic`/`wait retry` |
| 17 | 效果系统完整解析 `needs`/`given`/`with`/`perform`/`handle` | ✅ 已完成 | 中 | `Needs`/`Given`/`Handle`/`With` 全部解析完成，支持完整效果语法 |
| 18 | 补充完整单元测试 | ✅ 已完成 | 中 | 新增4个测试 (class/constructor, enum, trait, type_alias) |

## 完成度

**100%** - 所有 SPEC 语法支持已完成

## 验收标准

- [ ] 所有 SPEC.md 中定义的语法都能正确解析
- [ ] 生成正确的 AST
- [ ] 所有单元测试通过
- [ ] `examples/` 中所有示例都能通过语法检查

## 依赖

- x-lexer 必须完成
