[package]
name = "x-perceus"
version = "0.1.0"
edition = "2021"
description = "X语言 Perceus 兼容层（实现已迁移到 x-mir）"
license = "MIT"

[dependencies]
# 兼容层：真实实现位于 x-mir
x-mir = { workspace = true }

# 错误处理
thiserror = "1.0"

# 日志
log = "0.4"
