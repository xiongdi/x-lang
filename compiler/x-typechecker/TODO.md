# x-typechecker TODO

## 当前状态

- 基本框架已实现
- 支持基础类型和表达式检查
- 提供变量和函数声明检查
- 错误类型已定义（所有错误类型携带 span 位置信息）
- **新增**: 完整的类型比较支持（Option, Result, Tuple, Record, Union, Async, Generic 等）
- **新增**: Lambda 表达式类型推断
- **新增**: Record 表达式类型推断
- **新增**: Pipe 操作类型推断
- **新增**: Wait 操作类型推断
- **新增**: Needs/Given Effect 表达式支持
- **新增**: 类/特征/类型别名声明检查（基础框架）
- **新增**: 错误格式化模块（format.rs）

## 待办事项

### 1. 类型系统完善
- [x] 支持所有Type枚举变体
- [x] 完善类型比较（types_equal）
- [x] 实现类型兼容性检查
- [ ] 支持类型参数化和泛型
- [ ] 实现类型约束系统
- [x] 支持类型别名和类型定义

### 2. 类型推断增强
- [x] 完善复杂表达式类型推断
- [ ] 支持类型参数推断
- [ ] 实现上下文相关的类型推断
- [ ] 完善类型默认值
- [ ] 支持类型注解检查

### 3. 声明和定义检查
- [x] 完善类和接口声明检查（基础框架，等待 parser 支持）
- [ ] 支持模块和导入检查
- [x] 完善类型定义检查
- [ ] 支持泛型声明检查
- [x] 完善继承和实现检查（基础框架）

### 4. 语句检查
- [x] 完善Match语句类型检查
- [x] 完善Try语句类型检查
- [x] 完善For循环类型检查
- [x] 支持Break/Continue类型检查
- [x] 完善块语句检查

### 5. 表达式检查
- [x] 完善Lambda函数类型检查
- [x] 完善数组和字典类型检查
- [x] 支持记录类型检查
- [x] 完善管道操作类型检查
- [x] 支持异步操作类型检查
- [x] 完善Effect操作类型检查

### 6. 语义分析
- [x] 实现作用域检查（函数/分支/循环/try-catch 等引入新作用域）
- [x] 支持名称解析（变量/函数查找）
- [x] 检查未定义变量和函数（UndefinedVariable）
- [x] 检查重复声明（DuplicateDeclaration）
- [ ] 支持重载解析

### 7. 错误处理
- [x] 优化错误信息的精确性
- [x] 支持错误定位和代码片段（format.rs）
- [x] 提供错误修复建议
- [x] 支持多错误报告
- [x] 完善错误分类和代码

### 8. 性能优化
- [ ] 优化类型检查性能
- [ ] 实现增量类型检查
- [ ] 支持并行类型检查
- [ ] 优化内存使用

### 9. 测试覆盖
- [x] 添加单元测试模块（覆盖 ok path + UndefinedVariable + DuplicateDeclaration + 参数数量/类型不匹配 + if 条件类型 + match guard 类型）
- [ ] 测试所有边界条件
- [ ] 测试错误场景
- [ ] 性能基准测试

### 10. 其他
- [ ] 完善文档和示例
- [ ] 添加类型验证工具
- [ ] 支持类型可视化
- [ ] 集成Perceus分析

## 质量门禁（可测试与可验证）

### 覆盖率目标

- **行覆盖率**：100%
- **分支覆盖率**：100%
- **测试通过率**：100%

### 必须具备的测试类型

- [x] **单元测试**：覆盖 `type_check` 的主要分支（声明检查、语句检查、表达式推断、错误分支）
- [ ] **错误场景**：对每个 `TypeCheckError` 变体添加最小 AST/源码用例（并校验错误文本/类别）
- [ ] **回归用例**：每次修复类型推断/兼容性 bug 都新增最小复现测试

### 验收步骤（本地一键验证）

```bash
cd compiler
cargo test -p x-typechecker

# 覆盖率（line/branch）
cargo llvm-cov -p x-typechecker --tests
```

## 完成度估计

**整体完成度**：约 70%
- 基本功能：95%（作用域/名称解析/函数调用校验已补齐）
- 高级功能：45%（仍缺泛型、类型约束求解、重载解析）
- 错误处理：65%（错误携带 span，格式化模块已实现，修复建议已实现）
- 测试覆盖：55%

**当前测试覆盖**：本 crate 含 32 个单元测试；`cargo test -p x-typechecker` 全绿。覆盖率可用 `cargo llvm-cov -p x-typechecker --tests` 查看。

**未实现的核心功能**：
- ❌ 泛型类型参数推断
- ❌ 类型约束求解
- ❌ 重载解析
- ❌ 递归类型定义检查
