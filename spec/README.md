# X 语言规格说明书

> **本规格说明书是 X 语言的正式语法与语义定义。**
> 所有语言行为、语法规则和类型系统规则以本文档为准。
> 本规格说明书遵循 [DESIGN_GOALS.md](../DESIGN_GOALS.md) 中定义的设计目标——**DESIGN_GOALS.md 是 X 语言的最高准则与宪法性文件**，若本规格说明书与设计目标冲突，以设计目标为准。

---

## 语言概述

X 是一门**现代的、通用的编程语言**，适用于从底层系统编程到上层应用开发的全栈场景。其核心设计特征包括：

- **可读性第一**：所有关键字使用英文全称（`function`、`mutable`、`match`、`implement`），代码读起来像清晰的英文散文
- **类型安全**：Hindley-Milner 类型推断、代数数据类型、穷尽模式匹配——编译通过即无类型错误
- **无 null、无异常**：用 `Option<T>` 代替 null，用 `Result<T, E>` 代替异常，`?` 运算符传播错误，编译器强制处理所有路径
- **内存安全**：Perceus 编译时引用计数——无 GC 停顿、无手动管理、无生命周期标注，重用分析让函数式代码零分配原地更新
- **多范式**：函数式（纯函数、管道 `|>`、模式匹配）、面向对象（类、继承、trait）、过程式（`let mutable`、循环）、声明式（`where`/`sort by`）
- **效果系统**：函数副作用在类型签名中显式声明（`-> T with Effects`），`needs`/`given` 实现依赖注入
- **结构化并发**：`async`/`await`、`together`、`race`、`atomic`/`retry`
- **完整工具链**：`x` CLI 1:1 对标 Cargo（build、run、test、check、fmt、lint、doc、publish）
- **多后端架构**：C23 → 原生二进制、JVM 字节码、.NET CIL

---

## 目录

| 章节 | 标题 | 文件 |
|------|------|------|
| 第 0 章 | [设计哲学](docs/00-philosophy.md) | 设计目标、核心原则、关键字哲学、参考语言 |
| 第 1 章 | [词法结构](docs/01-lexical.md) | 字符集、关键字、标识符、字面量、运算符、注释 |
| 第 2 章 | [类型系统](docs/02-types.md) | 基本类型、复合类型、泛型、Option/Result、函数类型 |
| 第 3 章 | [表达式](docs/03-expressions.md) | 算术、逻辑、比较、管道、函数调用、成员访问 |
| 第 4 章 | [语句与声明](docs/04-statements.md) | 变量绑定、赋值、控制流、循环、模块声明 |
| 第 5 章 | [函数](docs/05-functions.md) | 函数定义、参数、返回值、Lambda、闭包、数学风格 |
| 第 6 章 | [类与接口](docs/06-classes.md) | 类、继承、trait、implement、属性、扩展 |
| 第 7 章 | [效果系统](docs/07-effects.md) | needs/given、效果声明、效果多态、处理器 |
| 第 8 章 | [模块系统](docs/08-modules.md) | 模块、导入、导出、包管理、访问控制 |
| 第 9 章 | [模式匹配](docs/09-patterns.md) | match 表达式、解构、守卫、穷尽检查 |
| 第 10 章 | [内存模型](docs/10-memory.md) | Perceus、dup/drop、重用分析、FBIP、weak 引用 |
| 第 11 章 | [元编程](docs/11-metaprogramming.md) | 编译期计算、宏、代码生成 |

---

## 排版约定

- **形式文法**使用 BNF/EBNF 风格，产生式用 `→` 分隔
- **代码示例**使用 ` ```x ` 标记的 X 语言代码块
- **类型规则**使用数学推导式（前提在横线上方，结论在下方）
- **集合定义**使用标准数学记号（∈、⊆、∪、∩、→）
- **中文说明**与形式定义交替出现，确保既精确又可读

---

*本规格说明书由 X 语言核心团队维护。*
