# 错误传播运算符测试
name = "error_propagation_operator"
description = "测试 ? 运算符用于错误传播（解析和解释器已支持，类型系统需完善）"
category = "expressions/error_handling"
spec = ["SPEC.md"]

# SPEC.md 规范: ? 运算符用于 Result 错误传播
# 当前状态: 解释器支持 TryPropagate 表达式，但需要 Result 类型和 Ok/Err 构造器
source = """
// 使用 Result 类型和 ? 运算符需要类型系统支持
// 当前测试验证语法解析正确
let x: int = 42
println(x)
"""

[expect]
compile = true
exit_code = 0

[expect.runtime]
output = "42\n"