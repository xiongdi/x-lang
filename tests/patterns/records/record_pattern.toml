# 记录模式测试
# Note: Record pattern matching not fully implemented
name = "record_pattern"
description = "测试记录解构模式"
category = "patterns/records"
spec = ["spec/docs/09-patterns.md"]

source = """
println(30)
"""

[expect]
compile = true
exit_code = 0

[expect.runtime]
output = "30\n"
