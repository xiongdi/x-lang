# TODO - tools/x-cli

## 待完成功能

### 高优先级

- [ ] 集成所有编译器阶段

### 中优先级

- [ ] 完善包管理命令

### 测试

- [ ] 添加单元测试

---

## 完成状态

- [x] CLI 框架（clap 命令定义）
- [x] `x run` 命令（解析 → 类型检查 → 解释执行）
- [x] `x check` 命令（语法和类型检查）
- [x] `x compile` 命令
- [x] `x build` 命令
- [x] `x test` 命令
- [x] `x bench` 命令
- [x] `x fmt` 命令
- [x] `x lint` 命令
- [x] `x doc` 命令
- [x] `x repl` 命令
- [x] 包管理命令（init、new、add、remove、update、vendor、package、publish 等）
- [x] 项目结构（Project 结构体）
- [x] 错误格式化